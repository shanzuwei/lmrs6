import { DashboardhostDirective } from './dashboardhost.directive';

describe('DashboardhostDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardhostDirective();
    expect(directive).toBeTruthy();
  });
});
